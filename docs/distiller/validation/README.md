# Distiller Validation

Generic Reasoning Distiller schemas, validators, fixtures, and validation tests have moved to `loteque/reasoning-distiller`.

Voxel-engine pins framework commit `fb7290622d6a9d929a059f111cdd60cd50496fcf`. Historical evaluation evidence remains in this repository and in the standalone voxel-engine reference corpus.

Active validation should consume the pinned standalone framework rather than add new generic implementation here.
