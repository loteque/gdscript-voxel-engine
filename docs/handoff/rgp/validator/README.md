# RGP Validator

The generic `rgp-validator/1` implementation has moved to `loteque/reasoning-distiller`.

Voxel-engine pins framework commit `fb7290622d6a9d929a059f111cdd60cd50496fcf`; the validator is `validators/rgp_validator.py` in that framework checkout.

Project submissions, evidence, dispositions, transactions, and canonical memory remain owned by this repository.
